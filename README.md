# ValutaTrade Hub

Консольное приложение для симуляции торговли валютами с поддержкой фиатных и криптовалют.

## Особенности

- 🏦 **Поддержка валют**: Фиатные (USD, EUR, GBP и др.) и криптовалюты (BTC, ETH и др.)
- 🔐 **Безопасность**: Хеширование паролей, защита от брутфорса
- 📊 **Портфель**: Отслеживание баланса и стоимости портфеля
- 💱 **Торговля**: Покупка/продажа валют с учетом комиссий
- 📈 **Курсы**: Кэширование курсов валют с TTL
- 📝 **Логирование**: Подробное логирование всех операций
- ⚙️ **Конфигурация**: Гибкая система настроек через YAML

## Установка

### Требования

- Python 3.8+
- Poetry (для управления зависимостями)

### Установка зависимостей

```bash
# Установите Poetry (если еще не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# Клонируйте репозиторий
git clone <repository-url>
cd finalproject_ivanov_pydv-001

# Установите зависимости
make install

# Создайте необходимые директории
make init-dirs

Использование
Запуск приложения
bash
make run

Основные команды
register --username <имя> --password <пароль> - Регистрация

login --username <имя> --password <пароль> - Вход

show-portfolio [--base <валюта>] - Портфель

buy --currency <код> --amount <сумма> - Купить

sell --currency <код> --amount <сумма> - Продать

get-rate --from <валюта> --to <валюта> - Курс

list-currencies - Список валют

transactions - История операций

settings - Настройки

Пример сессии

# Регистрация
register --username alice --password 1234 --email alice@example.com

# Вход
login --username alice --password 1234

# Пополнение баланса
deposit --currency USD --amount 1000

# Просмотр портфеля
show-portfolio --base USD --detailed

# Покупка BTC
buy --currency BTC --amount 0.01 --base USD

# Продажа ETH
sell --currency ETH --amount 0.1 --target USD

# Проверка курса
get-rate --from USD --to BTC

# История операций
transactions --limit 5